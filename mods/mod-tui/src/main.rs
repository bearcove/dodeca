//! Dodeca TUI plugin (dodeca-mod-tui)
//!
//! This is a "reverse plugin" - it connects to dodeca and subscribes to
//! event streams, rendering them in a terminal UI.

fn main() {
    todo!("TUI plugin implementation")
}
