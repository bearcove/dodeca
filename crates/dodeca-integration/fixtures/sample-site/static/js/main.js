// Test JavaScript file with string literals referencing assets
const imagePath = '/images/test.png';
const svgPath = '/images/test.svg';

console.log('Loading:', imagePath, svgPath);

document.addEventListener('DOMContentLoaded', function() {
    console.log('Page loaded');
});
