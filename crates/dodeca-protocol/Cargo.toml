[package]
name = "dodeca-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Shared protocol types for dodeca devtools"
publish = false

[dependencies]
facet = { git = "https://github.com/facet-rs/facet" }
facet-postcard = { git = "https://github.com/facet-rs/facet", features = ["alloc"] }
