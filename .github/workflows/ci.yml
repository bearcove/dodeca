# GENERATED BY: cargo xtask ci
# DO NOT EDIT - edit xtask/src/ci.rs instead

---
name: CI
"on":
  push:
    tags:
      - v*
    branches:
      - main
  pull_request:
    branches:
      - main
  merge_group:
    branches:
      - main
  workflow_dispatch: {}
permissions:
  contents: read
env:
  CARGO_TERM_COLOR: always
jobs:
  check-ci:
    name: Check CI up to date
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
      - name: Check CI files are up to date
        run: cargo xtask ci-github --check && cargo xtask ci-forgejo --check
  build-wasm:
    name: Build WASM
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
          targets: wasm32-unknown-unknown
      - name: Add WASM target
        run: rustup target add wasm32-unknown-unknown
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
      - name: Install wasm-pack
        run: cargo install wasm-pack --locked
      - name: Build embedded WASM
        run: cargo build --package dodeca
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
  clippy:
    name: Clippy
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 30
    needs:
      - build-wasm
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
          components: clippy
          targets: wasm32-unknown-unknown
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Download search WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
      - name: Clippy
        run: cargo clippy --all-features --all-targets -- -D warnings
  build-ddc-linux-x64:
    name: Build ddc (linux-x64)
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 30
    needs:
      - build-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Download search WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
      - name: Build ddc
        run: |-
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            export DODECA_RELEASE_VERSION="${GITHUB_REF_NAME}"
          fi
          cargo build --release --bin ddc --verbose
          actual="$(target/release/ddc --version)"
          echo "$actual"
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            expected="ddc ${GITHUB_REF_NAME#v}"
            if [[ "$actual" != "$expected" ]]; then
              echo "Expected '$expected', got '$actual'" >&2
              exit 1
            fi
          fi
        shell: bash
      - name: Test ddc
        run: |-
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            export DODECA_RELEASE_VERSION="${GITHUB_REF_NAME}"
          fi
          cargo test --release --bin ddc
        shell: bash
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ddc-linux-x64
          path: target/release/ddc
      - name: Install xz (macOS)
        run: if ! command -v xz >/dev/null 2>&1 && command -v brew >/dev/null 2>&1; then HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK=1 brew install xz; fi
      - name: Assemble archive
        run: bash scripts/assemble-archive.sh x86_64-unknown-linux-gnu
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-linux-x64
          path: dodeca-x86_64-unknown-linux-gnu.tar.xz
  integration-linux-x64:
    name: Integration (linux-x64)
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 30
    needs:
      - build-ddc-linux-x64
      - build-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Download search WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
      - name: Build integration-tests
        run: cargo build --package integration-tests
      - name: Download ddc
        uses: actions/download-artifact@v4
        with:
          name: ddc-linux-x64
          path: dist
      - name: Prepare artifacts
        run: chmod +x dist/ddc && ls -la dist/
      - name: Verify artifacts
        run: |
          #!/bin/bash
          set -euo pipefail

          echo 'Verifying all required artifacts exist in dist/'
          missing=0

          if [[ ! -x dist/ddc ]]; then
            echo '❌ MISSING: ddc'
            missing=1
          else
            echo '✓ ddc'
          fi

          if [[ $missing -eq 1 ]]; then
            echo ''
            echo 'ERROR: Some required binaries are missing!'
            echo 'This usually means a ddc build job failed or artifact upload was incomplete.'
            exit 1
          fi

          echo ''
          echo 'All artifacts verified.'
      - name: Run integration tests
        run: cargo xtask integration --no-build
        env:
          DODECA_BIN: ${{ github.workspace }}/dist/ddc
          DODECA_TEST_FIXTURES_DIR: ${{ github.workspace }}/crates/integration-tests/fixtures
  browser-tests-linux-x64:
    name: Browser Tests (linux-x64)
    runs-on:
      - ubuntu-24.04
    timeout-minutes: 30
    needs:
      - build-ddc-linux-x64
      - build-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download ddc
        uses: actions/download-artifact@v4
        with:
          name: ddc-linux-x64
          path: dist
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Prepare artifacts
        run: chmod +x dist/ddc && ls -la dist/
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install browser test dependencies
        run: cd browser-tests && npm ci && npx playwright install chromium --with-deps
      - name: Run browser tests
        run: cd browser-tests && npm test
        env:
          DODECA_BIN: ${{ github.workspace }}/dist/ddc
  build-ddc-macos-arm64:
    name: Build ddc (macos-arm64)
    runs-on:
      - macos-15
    timeout-minutes: 30
    needs:
      - build-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
          cache-bin: "false"
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Download search WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
      - name: Build ddc
        run: |-
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            export DODECA_RELEASE_VERSION="${GITHUB_REF_NAME}"
          fi
          cargo build --release --bin ddc --verbose
          actual="$(target/release/ddc --version)"
          echo "$actual"
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            expected="ddc ${GITHUB_REF_NAME#v}"
            if [[ "$actual" != "$expected" ]]; then
              echo "Expected '$expected', got '$actual'" >&2
              exit 1
            fi
          fi
        shell: bash
      - name: Test ddc
        run: |-
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && -n "${GITHUB_REF_NAME:-}" ]]; then
            export DODECA_RELEASE_VERSION="${GITHUB_REF_NAME}"
          fi
          cargo test --release --bin ddc
        shell: bash
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ddc-macos-arm64
          path: target/release/ddc
      - name: Install xz (macOS)
        run: if ! command -v xz >/dev/null 2>&1 && command -v brew >/dev/null 2>&1; then HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK=1 brew install xz; fi
      - name: Assemble archive
        run: bash scripts/assemble-archive.sh aarch64-apple-darwin
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-arm64
          path: dodeca-aarch64-apple-darwin.tar.xz
  integration-macos-arm64:
    name: Integration (macos-arm64)
    runs-on:
      - macos-15
    timeout-minutes: 30
    needs:
      - build-ddc-macos-arm64
      - build-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          cache-targets: "false"
          cache-bin: "false"
      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg
      - name: Download search WASM
        uses: actions/download-artifact@v4
        with:
          name: dodeca-search-wasm
          path: crates/dodeca-search-wasm/pkg
      - name: Build integration-tests
        run: cargo build --package integration-tests
      - name: Download ddc
        uses: actions/download-artifact@v4
        with:
          name: ddc-macos-arm64
          path: dist
      - name: Prepare artifacts
        run: chmod +x dist/ddc && ls -la dist/
      - name: Verify artifacts
        run: |
          #!/bin/bash
          set -euo pipefail

          echo 'Verifying all required artifacts exist in dist/'
          missing=0

          if [[ ! -x dist/ddc ]]; then
            echo '❌ MISSING: ddc'
            missing=1
          else
            echo '✓ ddc'
          fi

          if [[ $missing -eq 1 ]]; then
            echo ''
            echo 'ERROR: Some required binaries are missing!'
            echo 'This usually means a ddc build job failed or artifact upload was incomplete.'
            exit 1
          fi

          echo ''
          echo 'All artifacts verified.'
      - name: Run integration tests
        run: cargo xtask integration --no-build
        env:
          DODECA_BIN: ${{ github.workspace }}/dist/ddc
          DODECA_TEST_FIXTURES_DIR: ${{ github.workspace }}/crates/integration-tests/fixtures
  release:
    name: Release
    runs-on:
      - ubuntu-latest
    timeout-minutes: 30
    needs:
      - integration-linux-x64
      - integration-macos-arm64
    if: "startsWith(github.ref, 'refs/tags/')"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: build-*
          merge-multiple: "true"
      - name: List artifacts (before flatten)
        run: ls -laR dist/
      - name: Flatten artifact directories
        run: find dist -mindepth 2 -type f -exec mv -t dist {} + 2>/dev/null || true; find dist -mindepth 1 -type d -empty -delete 2>/dev/null || true
      - name: List artifacts (after flatten)
        run: ls -la dist/
      - name: Create GitHub Release
        run: |-
          shopt -s nullglob
          # Rename install.sh to dodeca-installer.sh for the release
          cp install.sh dist/dodeca-installer.sh
          gh release create "${{ github.ref_name }}" \
            --title "dodeca ${{ github.ref_name }}" \
            --generate-notes \
            dist/*.tar.xz dist/*.zip dist/dodeca-installer.sh
        shell: bash
      - name: Update Homebrew tap
        run: "bash scripts/update-homebrew.sh \"${{ github.ref_name }}\""
        shell: bash
