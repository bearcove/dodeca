# GENERATED BY: cargo xtask ci
# DO NOT EDIT - edit xtask/src/ci.rs instead

---
name: CI
"on": 
  push: 
    tags: 
      - v*

    branches: 
      - main

  pull_request: 
    branches: 
      - main

  workflow_dispatch: 
permissions: 
  contents: write

env: 
  CARGO_TERM_COLOR: always

jobs: 
  build-linux-x64: 
    name: Build (linux-x64)
    runs-on: depot-ubuntu-24.04-32
    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          components: clippy
          targets: wasm32-unknown-unknown

      - 
        name: Install Rust (nightly)
        uses: dtolnay/rust-toolchain@nightly
      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
        with: 
          cache-on-failure: "true"

      - 
        name: Install wasm-pack
        run: "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh"
      - 
        name: Build WASM
        run: cargo xtask wasm
      - 
        name: Build ddc
        run: cargo build --release -p dodeca
      - 
        name: Build cells
        run: cargo build --release -p cell-arborium --bin ddc-cell-arborium -p cell-code-execution --bin ddc-cell-code-execution -p cell-css --bin ddc-cell-css -p cell-fonts --bin ddc-cell-fonts -p cell-html --bin ddc-cell-html -p cell-html-diff --bin ddc-cell-html-diff -p cell-http --bin ddc-cell-http -p cell-image --bin ddc-cell-image -p cell-js --bin ddc-cell-js -p cell-jxl --bin ddc-cell-jxl -p cell-linkcheck --bin ddc-cell-linkcheck -p cell-markdown --bin ddc-cell-markdown -p cell-minify --bin ddc-cell-minify -p cell-pagefind --bin ddc-cell-pagefind -p cell-sass --bin ddc-cell-sass -p cell-svgo --bin ddc-cell-svgo -p cell-tui --bin ddc-cell-tui -p cell-webp --bin ddc-cell-webp
      - 
        name: Test ddc
        run: cargo test --release -p dodeca
      - 
        name: Test cells
        run: cargo test --release -p cell-arborium -p cell-code-execution -p cell-css -p cell-fonts -p cell-html -p cell-html-diff -p cell-http -p cell-image -p cell-js -p cell-jxl -p cell-linkcheck -p cell-markdown -p cell-minify -p cell-pagefind -p cell-sass -p cell-svgo -p cell-tui -p cell-webp
      - 
        name: Clippy
        run: cargo clippy --all-features --all-targets -- -D warnings
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh x86_64-unknown-linux-gnu
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-linux-x64
          path: dodeca-x86_64-unknown-linux-gnu.tar.xz


  build-macos-arm64: 
    name: Build (macos-arm64)
    runs-on: depot-macos-15
    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          components: clippy
          targets: wasm32-unknown-unknown

      - 
        name: Install Rust (nightly)
        uses: dtolnay/rust-toolchain@nightly
      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
        with: 
          cache-on-failure: "true"

      - 
        name: Install wasm-pack
        run: brew install wasm-pack
      - 
        name: Build WASM
        run: cargo xtask wasm
      - 
        name: Build ddc
        run: cargo build --release -p dodeca
      - 
        name: Build cells
        run: cargo build --release -p cell-arborium --bin ddc-cell-arborium -p cell-code-execution --bin ddc-cell-code-execution -p cell-css --bin ddc-cell-css -p cell-fonts --bin ddc-cell-fonts -p cell-html --bin ddc-cell-html -p cell-html-diff --bin ddc-cell-html-diff -p cell-http --bin ddc-cell-http -p cell-image --bin ddc-cell-image -p cell-js --bin ddc-cell-js -p cell-jxl --bin ddc-cell-jxl -p cell-linkcheck --bin ddc-cell-linkcheck -p cell-markdown --bin ddc-cell-markdown -p cell-minify --bin ddc-cell-minify -p cell-pagefind --bin ddc-cell-pagefind -p cell-sass --bin ddc-cell-sass -p cell-svgo --bin ddc-cell-svgo -p cell-tui --bin ddc-cell-tui -p cell-webp --bin ddc-cell-webp
      - 
        name: Test ddc
        run: cargo test --release -p dodeca
      - 
        name: Test cells
        run: cargo test --release -p cell-arborium -p cell-code-execution -p cell-css -p cell-fonts -p cell-html -p cell-html-diff -p cell-http -p cell-image -p cell-js -p cell-jxl -p cell-linkcheck -p cell-markdown -p cell-minify -p cell-pagefind -p cell-sass -p cell-svgo -p cell-tui -p cell-webp
      - 
        name: Clippy
        run: cargo clippy --all-features --all-targets -- -D warnings
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh aarch64-apple-darwin
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-macos-arm64
          path: dodeca-aarch64-apple-darwin.tar.xz


  integration-linux-x64: 
    name: Integration (linux-x64)
    runs-on: depot-ubuntu-24.04-32
    needs: 
      - build-linux-x64

    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
        with: 
          cache-on-failure: "true"

      - 
        name: Download build
        uses: actions/download-artifact@v4
        with: 
          name: build-linux-x64
          path: dist

      - 
        name: Extract archive
        run: tar -xf dist/dodeca-x86_64-unknown-linux-gnu.tar.xz -C dist && ls -la dist/
      - 
        name: Run integration tests
        run: "cargo test --release -p dodeca-integration --test '*'"
        env: 
          DODECA_BIN: ${{ github.workspace }}/dist/ddc
          DODECA_CELLS: ${{ github.workspace }}/dist


  integration-macos-arm64: 
    name: Integration (macos-arm64)
    runs-on: depot-macos-15
    needs: 
      - build-macos-arm64

    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
        with: 
          cache-on-failure: "true"

      - 
        name: Download build
        uses: actions/download-artifact@v4
        with: 
          name: build-macos-arm64
          path: dist

      - 
        name: Extract archive
        run: tar -xf dist/dodeca-aarch64-apple-darwin.tar.xz -C dist && ls -la dist/
      - 
        name: Run integration tests
        run: "cargo test --release -p dodeca-integration --test '*'"
        env: 
          DODECA_BIN: ${{ github.workspace }}/dist/ddc
          DODECA_CELLS: ${{ github.workspace }}/dist


  release: 
    name: Release
    runs-on: ubuntu-latest
    needs: 
      - build-linux-x64
      - build-macos-arm64

    if: "startsWith(github.ref, 'refs/tags/')"
    env: 
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Download all artifacts
        uses: actions/download-artifact@v4
        with: 
          path: dist
          pattern: build-*
          merge-multiple: "true"

      - 
        name: List artifacts
        run: ls -laR dist/
      - 
        name: Create GitHub Release
        run: "gh release create \"${{ github.ref_name }}\" \\\n  --title \"dodeca ${{ github.ref_name }}\" \\\n  --generate-notes \\\n  dist/**/*.tar.xz dist/**/*.zip"
        shell: bash
      - 
        name: Update Homebrew tap
        run: "bash scripts/update-homebrew.sh \"${{ github.ref_name }}\""
        shell: bash

