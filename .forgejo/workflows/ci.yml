# GENERATED BY: cargo xtask ci
# DO NOT EDIT - edit xtask/src/ci.rs instead

---
name: CI
"on": 
  push: 
    tags: 
      - v*

    branches: 
      - main

  pull_request: 
    branches: 
      - main

  workflow_dispatch: 
permissions: 
  contents: write

env: 
  CARGO_TERM_COLOR: always

jobs: 
  clippy: 
    name: Clippy
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    continue-on-error: true
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
        with: 
          components: clippy
          targets: wasm32-unknown-unknown

      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-ci-linux"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Clippy
        run: cargo clippy --all-features --all-targets -- -D warnings

  build-wasm: 
    name: Build WASM
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
        with: 
          targets: wasm32-unknown-unknown

      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-ci-linux"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Install wasm-pack
        run: "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh"
      - 
        name: Build WASM
        run: cargo xtask wasm
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg


  build-ddc-linux-x64: 
    name: Build ddc (linux-x64)
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-ddc-linux-x64"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build ddc
        run: cargo build --release -p dodeca
      - 
        name: Test ddc
        run: cargo test --release -p dodeca --bins
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: ddc-linux-x64
          path: target/release/ddc


  build-cells-linux-x64-1: 
    name: Build cells (linux-x64) [cell-arborium, cell-code-execution, cell-css, cell-fonts, cell-html, cell-html-diff, cell-http, cell-image, cell-js]
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-targets-cells-linux-x64-1"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build cells
        run: cargo build --release -p cell-arborium --bin ddc-cell-arborium -p cell-code-execution --bin ddc-cell-code-execution -p cell-css --bin ddc-cell-css -p cell-fonts --bin ddc-cell-fonts -p cell-html --bin ddc-cell-html -p cell-html-diff --bin ddc-cell-html-diff -p cell-http --bin ddc-cell-http -p cell-image --bin ddc-cell-image -p cell-js --bin ddc-cell-js
      - 
        name: Test cells
        run: cargo test --release -p cell-arborium -p cell-code-execution -p cell-css -p cell-fonts -p cell-html -p cell-html-diff -p cell-http -p cell-image -p cell-js
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: cells-linux-x64-1
          path: "target/release/ddc-cell-arborium\ntarget/release/ddc-cell-code-execution\ntarget/release/ddc-cell-css\ntarget/release/ddc-cell-fonts\ntarget/release/ddc-cell-html\ntarget/release/ddc-cell-html-diff\ntarget/release/ddc-cell-http\ntarget/release/ddc-cell-image\ntarget/release/ddc-cell-js"


  build-cells-linux-x64-2: 
    name: Build cells (linux-x64) [cell-jxl, cell-linkcheck, cell-markdown, cell-minify, cell-pagefind, cell-sass, cell-svgo, cell-tui, cell-webp]
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-targets-cells-linux-x64-2"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build cells
        run: cargo build --release -p cell-jxl --bin ddc-cell-jxl -p cell-linkcheck --bin ddc-cell-linkcheck -p cell-markdown --bin ddc-cell-markdown -p cell-minify --bin ddc-cell-minify -p cell-pagefind --bin ddc-cell-pagefind -p cell-sass --bin ddc-cell-sass -p cell-svgo --bin ddc-cell-svgo -p cell-tui --bin ddc-cell-tui -p cell-webp --bin ddc-cell-webp
      - 
        name: Test cells
        run: cargo test --release -p cell-jxl -p cell-linkcheck -p cell-markdown -p cell-minify -p cell-pagefind -p cell-sass -p cell-svgo -p cell-tui -p cell-webp
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: cells-linux-x64-2
          path: "target/release/ddc-cell-jxl\ntarget/release/ddc-cell-linkcheck\ntarget/release/ddc-cell-markdown\ntarget/release/ddc-cell-minify\ntarget/release/ddc-cell-pagefind\ntarget/release/ddc-cell-sass\ntarget/release/ddc-cell-svgo\ntarget/release/ddc-cell-tui\ntarget/release/ddc-cell-webp"


  integration-linux-x64: 
    name: Integration (linux-x64)
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    needs: 
      - build-ddc-linux-x64
      - build-cells-linux-x64-1
      - build-cells-linux-x64-2

    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-integration-linux-x64"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Download ddc
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: ddc-linux-x64
          path: dist

      - 
        name: Download cells
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          pattern: cells-linux-x64-*
          path: dist
          merge-multiple: "true"

      - 
        name: Prepare binaries
        run: chmod +x dist/ddc* && ls -la dist/
      - 
        name: Run integration tests
        run: cargo xtask integration --no-build
        env: 
          DODECA_BIN: ${{ gitea.workspace }}/dist/ddc
          DODECA_CELL_PATH: ${{ gitea.workspace }}/dist


  assemble-linux-x64: 
    name: Assemble (linux-x64)
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    needs: 
      - integration-linux-x64
      - build-wasm

    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Download ddc
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: ddc-linux-x64
          path: target/release

      - 
        name: Download cells
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          pattern: cells-linux-x64-*
          path: target/release
          merge-multiple: "true"

      - 
        name: Download WASM
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg

      - 
        name: Install xz (macOS)
        run: if command -v brew >/dev/null 2>&1; then HOMEBREW_NO_AUTO_UPDATE=1 brew install xz; fi
      - 
        name: List binaries
        run: ls -la target/release/
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh x86_64-unknown-linux-gnu
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: build-linux-x64
          path: dodeca-x86_64-unknown-linux-gnu.tar.xz


  build-ddc-macos-arm64: 
    name: Build ddc (macos-arm64)
    runs-on: 
      - mac-arm64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-ddc-macos-arm64"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build ddc
        run: cargo build --release -p dodeca
      - 
        name: Test ddc
        run: cargo test --release -p dodeca --bins
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: ddc-macos-arm64
          path: target/release/ddc


  build-cells-macos-arm64-1: 
    name: Build cells (macos-arm64) [cell-arborium, cell-code-execution, cell-css, cell-fonts, cell-html, cell-html-diff, cell-http, cell-image, cell-js]
    runs-on: 
      - mac-arm64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-targets-cells-macos-arm64-1"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build cells
        run: cargo build --release -p cell-arborium --bin ddc-cell-arborium -p cell-code-execution --bin ddc-cell-code-execution -p cell-css --bin ddc-cell-css -p cell-fonts --bin ddc-cell-fonts -p cell-html --bin ddc-cell-html -p cell-html-diff --bin ddc-cell-html-diff -p cell-http --bin ddc-cell-http -p cell-image --bin ddc-cell-image -p cell-js --bin ddc-cell-js
      - 
        name: Test cells
        run: cargo test --release -p cell-arborium -p cell-code-execution -p cell-css -p cell-fonts -p cell-html -p cell-html-diff -p cell-http -p cell-image -p cell-js
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: cells-macos-arm64-1
          path: "target/release/ddc-cell-arborium\ntarget/release/ddc-cell-code-execution\ntarget/release/ddc-cell-css\ntarget/release/ddc-cell-fonts\ntarget/release/ddc-cell-html\ntarget/release/ddc-cell-html-diff\ntarget/release/ddc-cell-http\ntarget/release/ddc-cell-image\ntarget/release/ddc-cell-js"


  build-cells-macos-arm64-2: 
    name: Build cells (macos-arm64) [cell-jxl, cell-linkcheck, cell-markdown, cell-minify, cell-pagefind, cell-sass, cell-svgo, cell-tui, cell-webp]
    runs-on: 
      - mac-arm64-metal

    timeout-minutes: 30
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-targets-cells-macos-arm64-2"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Build cells
        run: cargo build --release -p cell-jxl --bin ddc-cell-jxl -p cell-linkcheck --bin ddc-cell-linkcheck -p cell-markdown --bin ddc-cell-markdown -p cell-minify --bin ddc-cell-minify -p cell-pagefind --bin ddc-cell-pagefind -p cell-sass --bin ddc-cell-sass -p cell-svgo --bin ddc-cell-svgo -p cell-tui --bin ddc-cell-tui -p cell-webp --bin ddc-cell-webp
      - 
        name: Test cells
        run: cargo test --release -p cell-jxl -p cell-linkcheck -p cell-markdown -p cell-minify -p cell-pagefind -p cell-sass -p cell-svgo -p cell-tui -p cell-webp
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: cells-macos-arm64-2
          path: "target/release/ddc-cell-jxl\ntarget/release/ddc-cell-linkcheck\ntarget/release/ddc-cell-markdown\ntarget/release/ddc-cell-minify\ntarget/release/ddc-cell-pagefind\ntarget/release/ddc-cell-sass\ntarget/release/ddc-cell-svgo\ntarget/release/ddc-cell-tui\ntarget/release/ddc-cell-webp"


  integration-macos-arm64: 
    name: Integration (macos-arm64)
    runs-on: 
      - mac-arm64-metal

    timeout-minutes: 30
    needs: 
      - build-ddc-macos-arm64
      - build-cells-macos-arm64-1
      - build-cells-macos-arm64-2

    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Install Rust
        uses: "https://github.com/dtolnay/rust-toolchain@stable"
      - 
        name: Cache
        uses: "https://data.forgejo.org/actions/cache@v4"
        with: 
          path: target
          key: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-integration-macos-arm64"
          restore-keys: "${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-"

      - 
        name: Download ddc
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: ddc-macos-arm64
          path: dist

      - 
        name: Download cells
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          pattern: cells-macos-arm64-*
          path: dist
          merge-multiple: "true"

      - 
        name: Prepare binaries
        run: chmod +x dist/ddc* && ls -la dist/
      - 
        name: Run integration tests
        run: cargo xtask integration --no-build
        env: 
          DODECA_BIN: ${{ gitea.workspace }}/dist/ddc
          DODECA_CELL_PATH: ${{ gitea.workspace }}/dist


  assemble-macos-arm64: 
    name: Assemble (macos-arm64)
    runs-on: 
      - mac-arm64-metal

    timeout-minutes: 30
    needs: 
      - integration-macos-arm64
      - build-wasm

    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Download ddc
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: ddc-macos-arm64
          path: target/release

      - 
        name: Download cells
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          pattern: cells-macos-arm64-*
          path: target/release
          merge-multiple: "true"

      - 
        name: Download WASM
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          name: dodeca-devtools-wasm
          path: crates/dodeca-devtools/pkg

      - 
        name: Install xz (macOS)
        run: if command -v brew >/dev/null 2>&1; then HOMEBREW_NO_AUTO_UPDATE=1 brew install xz; fi
      - 
        name: List binaries
        run: ls -la target/release/
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh aarch64-apple-darwin
      - 
        name: Upload artifact
        uses: "https://code.forgejo.org/actions/upload-artifact@v3"
        with: 
          name: build-macos-arm64
          path: dodeca-aarch64-apple-darwin.tar.xz


  release: 
    name: Release
    runs-on: 
      - linux-amd64-metal

    timeout-minutes: 30
    needs: 
      - assemble-linux-x64
      - assemble-macos-arm64

    if: "startsWith(gitea.ref, 'refs/tags/')"
    steps: 
      - 
        name: Checkout
        uses: "https://github.com/actions/checkout@v4"
      - 
        name: Download all artifacts
        uses: "https://code.forgejo.org/actions/download-artifact@v3"
        with: 
          path: dist
          pattern: build-*
          merge-multiple: "true"

      - 
        name: List artifacts
        run: ls -laR dist/
      - 
        name: Show release info
        run: "echo \"Release: ${{ gitea.ref_name }}\"; ls -la dist/"

