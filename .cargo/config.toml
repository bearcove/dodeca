[alias]
xtask = "run --package xtask --"

# # Unstable features (requires nightly or cargo +nightly)
# [unstable]
# profile-rustflags = true

# # Enable frame pointers for the mystery profile (better stack traces)
# [profile.mystery]
# rustflags = ["-C", "force-frame-pointers=yes"]
